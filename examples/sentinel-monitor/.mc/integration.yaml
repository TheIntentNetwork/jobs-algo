version: 1
package: "@intent-network/sentinel-monitor"
domain: monitoring
subdomain: observability

job_types:
  - path: "job-types/*.yaml"
workflows:
  - path: "workflows/*.yaml"
agents:
  - path: "agents/*.yaml"

providers:
  allowed: [local-process]
  default: local-process

llm_providers:
  allowed: [ollama-local]
  default: ollama-local