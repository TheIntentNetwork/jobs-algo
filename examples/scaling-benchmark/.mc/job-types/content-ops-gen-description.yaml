type: gen-description
description: Scaling benchmark job for content-ops/publishing - gen-description inference
domain: content-ops
subdomain: publishing
loop:
  mode: interval
  interval_sec: 10
  max_iterations: 1
  iteration_timeout_sec: 120
  overall_timeout_sec: 180
  on_max_reached: completed
prompt_template: |
  {prompt}
required_capability: general-purpose
