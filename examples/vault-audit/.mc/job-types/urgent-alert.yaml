type: alert
description: Urgent security alert
domain: security
subdomain: compliance
loop:
  mode: interval
  interval_sec: 10
  max_iterations: 1
  iteration_timeout_sec: 120
  overall_timeout_sec: 180
  on_max_reached: completed
prompt_template: |
  {prompt}
required_capability: general-purpose