version: 1
package: "@intent-network/mileage-benchmark"
domain: benchmark
subdomain: mileage

job_types:
  - path: "job-types/*.yaml"
workflows:
  - path: "workflows/*.yaml"
agents:
  - path: "agents/*.yaml"

providers:
  allowed: [local-process]
  default: local-process

llm_providers:
  allowed: [ollama-local]
  default: ollama-local
